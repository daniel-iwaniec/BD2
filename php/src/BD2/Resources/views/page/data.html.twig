{% extends 'layout.html.twig' %}
{% set title = 'Dane' %}

{% block content %}
    <div class="jumbotron">
        <h1>Bazy Danych&nbsp;2 <sup><small><span class="label label-default">Projekt</span></small></sup></h1>

        <p>
            Lorem ipsum dolor sit amet.
        </p>
        <p><a href="{{ url('import') }}" class="btn btn-primary btn-lg" role="button" data-toggle="tooltip" data-placement="right" title="za pomocą sqlloader'a">Zobacz import &raquo;</a></p>
    </div>

    <div class="page-header"><h1>Dane</h1></div>
    <h2><sup><small><span class="label label-primary">wymiar klient</span></small></sup> Dane tabeli <strong>branza</strong> <span class="badge">5 rekordów</span></h2>
    {{ render(url('table', {'table': 'branza'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar klient</span></small></sup> Dane tabeli <strong>klient</strong> <span class="badge">100 rekordów</span></h2>
    {{ render(url('table', {'table': 'klient'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar sprzedawca</span></small></sup> Dane tabeli <strong>stanowisko</strong> <span class="badge">5 rekordów</span></h2>
    {{ render(url('table', {'table': 'stanowisko'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar sprzedawca</span></small></sup> Dane tabeli <strong>sprzedawca</strong> <span class="badge">50 rekordów</span></h2>
    {{ render(url('table', {'table': 'sprzedawca'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar data_sprzedazy</span></small></sup> Dane tabeli <strong>rok</strong> <span class="badge">5 rekordów</span></h2>
    {{ render(url('table', {'table': 'rok'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar data_sprzedazy</span></small></sup> Dane tabeli <strong>miesiac</strong> <span class="badge">60 rekordów</span></h2>
    {{ render(url('table', {'table': 'miesiac'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar data_sprzedazy</span></small></sup> Dane tabeli <strong>data_sprzedazy</strong> <span class="badge">1826 rekordów</span></h2>
    {{ render(url('table', {'table': 'data_sprzedazy'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar produkt</span></small></sup> Dane tabeli <strong>produkt_typ</strong> <span class="badge">24 rekordy</span></h2>
    {{ render(url('table', {'table': 'produkt_typ'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar produkt</span></small></sup> Dane tabeli <strong>produkt</strong> <span class="badge">1190 rekordów</span></h2>
    {{ render(url('table', {'table': 'produkt'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar lokalizacja</span></small></sup> Dane tabeli <strong>wojewodztwo</strong> <span class="badge">16 rekordów</span></h2>
    {{ render(url('table', {'table': 'wojewodztwo'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar lokalizacja</span></small></sup> Dane tabeli <strong>miasto</strong> <span class="badge">32 rekordy</span></h2>
    {{ render(url('table', {'table': 'miasto'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-primary">wymiar lokalizacja</span></small></sup> Dane tabeli <strong>lokalizacja</strong> <span class="badge">64 rekordy</span></h2>
    {{ render(url('table', {'table': 'lokalizacja'})) }}
    <div class="well">Opis tych danych.</div>

    <h2><sup><small><span class="label label-success">tabela faktów</span></small></sup> Dane tabeli <strong>sprzedaz</strong> <span class="badge">5000 rekordów</span></h2>
    {{ render(url('table', {'table': 'sprzedaz'})) }}
    <div class="well">Opis tych danych.</div>
{% endblock %}
